# Vue without build

A template project that creates a webpage in Vue without building (no node, no npm, no config)

Needs an HTTP static file server for ES modules

## Work offline

Download library scripts in `index.html` from npm or cdn to the `lib` directory
